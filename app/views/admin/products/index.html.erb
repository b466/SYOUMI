<h1>Admin::Products#index</h1>
<p>Find me in app/views/admin/products/index.html.erb</p>

<% @products.each do |product| %>
		<%= product.id %>
		<%= link_to product.name, admin_product_path(product) %>
		<%= product.price %>
		<% end %>